# libxcb - X protocol C-language binding
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxcb
version=1.18.0
release=1
desc="X protocol C-language binding"
url="https://x.org/"
license="MIT"
depend="glibc libxau libxdmcp"
makedepend=""
source="https://x.org/pub/individual/lib/libxcb-1.18.0.tar.xz"
sha256="SKIP"

build() {
	cd "libxcb-1.18.0"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libxcb-1.18.0"
	make DESTDIR="$pkgdir" install
}
