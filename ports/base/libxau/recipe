# libxau - X authorisation library
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxau
version=1.0.12
release=1
desc="X authorisation library"
url="https://x.org/"
license="MIT"
depend="glibc"
makedepend=""
source="https://x.org/pub/individual/lib/libXau-1.0.12.tar.xz"
sha256="SKIP"

build() {
	cd "libXau-1.0.12"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libXau-1.0.12"
	make DESTDIR="$pkgdir" install
}
