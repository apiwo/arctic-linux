# libxres - X Resource extension library
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxres
version=1.2.3
release=1
desc="X Resource extension library"
url="https://x.org/"
license="MIT"
depend="libx11 libxext"
makedepend=""
source="https://x.org/pub/individual/lib/libXres-1.2.3.tar.xz"
sha256="SKIP"

build() {
	cd "libXres-1.2.3"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libXres-1.2.3"
	make DESTDIR="$pkgdir" install
}
