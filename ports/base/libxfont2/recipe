# libxfont2 - X font library
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxfont2
version=2.0.7
release=1
desc="X font library"
url="https://x.org/"
license="MIT"
depend="glibc freetype libfontenc"
makedepend=""
source="https://x.org/pub/individual/lib/libXfont2-2.0.7.tar.xz"
sha256="SKIP"

build() {
	cd "libXfont2-2.0.7"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libXfont2-2.0.7"
	make DESTDIR="$pkgdir" install
}
