# libxshmfence - Shared-memory fences
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxshmfence
version=1.3.3
release=1
desc="Shared-memory fences"
url="https://x.org/"
license="MIT"
depend="glibc"
makedepend=""
source="https://x.org/pub/individual/lib/libxshmfence-1.3.3.tar.xz"
sha256="SKIP"

build() {
	cd "libxshmfence-1.3.3"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libxshmfence-1.3.3"
	make DESTDIR="$pkgdir" install
}
