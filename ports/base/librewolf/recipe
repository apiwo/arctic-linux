# librewolf - Firefox without the telemetry
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=librewolf
version=147.0
release=1
desc="Firefox without the telemetry"
url="https://librewolf.net/"
license="MPL-2.0"
depend="glibc gtk3 mesa pipewire ffmpeg nss nspr icu"
makedepend="rust cargo nodejs python nasm"
source="https://gitlab.com/librewolf-community/browser/source/-/archive/147.0/source-147.0.tar.gz"
sha256="SKIP"

build() {
	cd "source-147.0"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "source-147.0"
	make DESTDIR="$pkgdir" install
}
