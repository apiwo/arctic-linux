# xfce4-session - The Xfce session manager
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=xfce4-session
version=4.22.0
release=1
desc="The Xfce session manager"
url="https://xfce.org/"
license="GPL-2.0-or-later"
depend="gtk3 libxfce4ui libwnck"
makedepend=""
source="https://archive.xfce.org/src/xfce/xfce4-session/4.22/xfce4-session-4.22.0.tar.bz2"
sha256="SKIP"

build() {
	cd "xfce4-session-4.22.0"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "xfce4-session-4.22.0"
	make DESTDIR="$pkgdir" install
}
