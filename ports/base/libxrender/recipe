# libxrender - X Render extension library
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxrender
version=0.9.12
release=1
desc="X Render extension library"
url="https://x.org/"
license="MIT"
depend="glibc libx11"
makedepend=""
source="https://x.org/pub/individual/lib/libXrender-0.9.12.tar.xz"
sha256="SKIP"

build() {
	cd "libXrender-0.9.12"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libXrender-0.9.12"
	make DESTDIR="$pkgdir" install
}
