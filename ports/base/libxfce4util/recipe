# libxfce4util - Xfce utility library
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxfce4util
version=4.22.0
release=1
desc="Xfce utility library"
url="https://xfce.org/"
license="LGPL-2.0-or-later"
depend="glib"
makedepend=""
source="https://archive.xfce.org/src/xfce/libxfce4util/4.22/libxfce4util-4.22.0.tar.bz2"
sha256="SKIP"

build() {
	cd "libxfce4util-4.22.0"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libxfce4util-4.22.0"
	make DESTDIR="$pkgdir" install
}
