# libxaw - X Athena widget set
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxaw
version=1.0.16
release=1
desc="X Athena widget set"
url="https://x.org/"
license="MIT"
depend="glibc libx11 libxext libxmu"
makedepend=""
source="https://x.org/pub/individual/lib/libXaw-1.0.16.tar.xz"
sha256="SKIP"

build() {
	cd "libXaw-1.0.16"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libXaw-1.0.16"
	make DESTDIR="$pkgdir" install
}
