# libxt - X Toolkit Intrinsics
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxt
version=1.3.1
release=1
desc="X Toolkit Intrinsics"
url="https://x.org/"
license="MIT"
depend="libx11 libsm"
makedepend=""
source="https://x.org/pub/individual/lib/libXt-1.3.1.tar.xz"
sha256="SKIP"

build() {
	cd "libXt-1.3.1"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libXt-1.3.1"
	make DESTDIR="$pkgdir" install
}
