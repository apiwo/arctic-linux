# libxext - X miscellaneous extensions
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libxext
version=1.3.7
release=1
desc="X miscellaneous extensions"
url="https://x.org/"
license="MIT"
depend="glibc libx11"
makedepend=""
source="https://x.org/pub/individual/lib/libXext-1.3.7.tar.xz"
sha256="SKIP"

build() {
	cd "libXext-1.3.7"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libXext-1.3.7"
	make DESTDIR="$pkgdir" install
}
