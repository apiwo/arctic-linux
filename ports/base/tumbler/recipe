# tumbler - Thumbnail service
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=tumbler
version=4.22.0
release=1
desc="Thumbnail service"
url="https://xfce.org/"
license="LGPL-2.0-or-later"
depend="glib gdk-pixbuf libpng freetype"
makedepend=""
source="https://archive.xfce.org/src/xfce/tumbler/4.22/tumbler-4.22.0.tar.bz2"
sha256="SKIP"

build() {
	cd "tumbler-4.22.0"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "tumbler-4.22.0"
	make DESTDIR="$pkgdir" install
}
