# libice - X Inter-Client Exchange library
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libice
version=1.1.2
release=1
desc="X Inter-Client Exchange library"
url="https://x.org/"
license="MIT"
depend="glibc"
makedepend=""
source="https://x.org/pub/individual/lib/libICE-1.1.2.tar.xz"
sha256="SKIP"

build() {
	cd "libICE-1.1.2"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libICE-1.1.2"
	make DESTDIR="$pkgdir" install
}
