# mtr - Combined traceroute and ping
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=mtr
version=0.96
release=1
desc="Combined traceroute and ping"
url="https://www.bitwizard.nl/mtr/"
license="GPL-2.0-only"
depend="glibc netbsd-curses libcap"
makedepend=""
source="https://github.com/traviscross/mtr/archive/refs/tags/v0.96.tar.gz"
sha256="SKIP"

build() {
	cd "mtr-0.96"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "mtr-0.96"
	make DESTDIR="$pkgdir" install
}
