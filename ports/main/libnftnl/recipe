# libnftnl - Low-level nftables library
# Generated from manifest.tsv by gen-ports.py. Edit the manifest, not this file,
# unless the package needs something the template cannot express - in which case
# drop a `recipe.local` beside it and it will be preserved.
name=libnftnl
version=1.3.0
release=1
desc="Low-level nftables library"
url="https://netfilter.org/projects/libnftnl/"
license="GPL-2.0-or-later"
depend="glibc libmnl"
makedepend=""
source="https://netfilter.org/projects/libnftnl/files/libnftnl-1.3.0.tar.xz"
sha256="SKIP"

build() {
	cd "libnftnl-1.3.0"
	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libdir=/usr/lib \
		--disable-static \
		--disable-nls
	make -j"$JOBS"
}

package() {
	cd "libnftnl-1.3.0"
	make DESTDIR="$pkgdir" install
}
