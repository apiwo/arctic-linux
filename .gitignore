# Build output lives outside the tree (../arctic-build), but just in case:
*.iso
*.iso.sha256
*.alpmz
*.tar.xz
*.tar.gz
*.tar.bz2

# Generated by branding/gen-branding.py from arctic-logo-master.png.
# The master and the generator are tracked; the derived set is not, except the
# assets the ISO needs, which are listed below.
branding/icons/
branding/wallpaper/
branding/misc/

# Editor and OS noise
*.swp
*~
.DS_Store
