-- LazyVim, bootstrapped on first launch.
--
-- lazy.nvim and the plugin set are not shipped in the image - they are
-- cloned the first time nvim starts, which needs a working network. Without
-- one nvim still opens as plain neovim with the options below; nothing here
-- errors out on a machine that is still offline mid-install.
require("config.lazy")
