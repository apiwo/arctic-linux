# ~/.zshrc - your own zsh settings.
# The system configuration in /etc/zsh/zshrc has already been loaded, so this
# file is only for what you want to change.

# Uncomment for vi keys instead of emacs keys.
# bindkey -v

# Your aliases.
# alias v=nvim
